---
###############################################################################
# Start
###############################################################################
# Start services and confirm they are active and running.

- name: 'Start | systemd | whisparr.service'
  ansible.builtin.systemd:
    name: 'whisparr.service'
    state: 'started'
    enabled: true
    daemon_reload: true
  delay: 15
  retries: 3
  register: whisparr__service
  until: whisparr__service.status.ActiveState == 'active'

- name: 'Start | fact | query services'
  ansible.builtin.service_facts:

- name: 'Start | assert | services running'
  ansible.builtin.assert:
    quiet: true
    that: ansible_facts.services[item].state == 'running'
    fail_msg: |

      ✘ Service failed to start.
      {{ item }}
  loop:
    - 'whisparr.service'
