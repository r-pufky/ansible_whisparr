---
###############################################################################
# Feature Flags
###############################################################################

# 1. Backup config to whisparr_cfg_backup_dir?
#
# Backup existing config data directory and exit role. Creates archive of
# /var/opt/whisparr. whisparr_cfg_backup_dir created if non-existent. Services
# stopped, files backed up, services started.
#
# Default: False.
whisparr_flg_backup: false

# 2. Restore config from backup?
#
# Restore config data directory and exit role. Extracts latest backup archive
# to /var/opt/whisparr. Services stopped, files backed up, services started.
#
# Default: False.
whisparr_flg_restore: false

# 3. Run maintenance tasks?
#
# Preform role maintenance tasks including removal of old role versions.
#
# Default: True.
whisparr_flg_maintenance: true

# 1. Install application?
#
# Creates required default directories, users, install packages, download
# software, autogenerates config.xml if not specified, update and start
# services.
#
# Default: True.
whisparr_flg_install: true

# 2. Set configuration data?
#
# Overwrite config data directory sourced from whisparr_cfg_dir. Use backup and
# restore to manage configuration data. Stops service, deploys user config
# data, updates and starts services.
#
# Default: False.
whisparr_flg_config: false

###############################################################################
# Service
###############################################################################

# Whisparr Release.
#
# Versions (Schematic):
#       MAJOR: Unsafe - Usually requires major role changes; only default MAJOR
#              version is supported.
#       MINOR: Safe - Usually requires no role updates. Changes usually
#              constrained to internal changes requiring no config file or
#              dependency changes.
#       PATCH: Safe - Usually requires no role updates.
#   RECOMMEND: Only increment MINOR.PATCH.
#
# Default: 'v2.2.0-release.108'.
#
# Reference:
# * https://github.com/Whisparr/Whisparr/releases/latest
whisparr_srv_version: 'v2.2.0-release.108'

# Github access token (whisparr).
#
# Unauthenticated (anonymous) API requests are heavily restricted to the sum
# total of requests to github from a given IP. Use an access token if
# receiving 403's.
#
# Classic Tokens:
# * settings > developer settings > personal access tokens (classic)
#   * notes: {LABEL APPROPRIATELY}
#   * expiration: {CUSTOM}
#   * scopes: NONE
#
# Fine-grained Tokens:
# * settings > developer settings > personal access tokens (fine-grained)
#   * token name: {LABEL APPROPRIATELY}
#   * expiration: {CUSTOM}
#   * description: {DESCRIPTIVE TEXT}
#   * resource owner: {YOUR ACCOUNT}
#   * repository access: Public Repositories (read-only)
#
# Default: '' (unauthenticated).
#
# Reference:
# * https://docs.github.com/en/authentication
whisparr_srv_token: ''

# User that Whisparr will run under.
#
# 'media' will automatically be created using vars/main.yml if required.
# All other accounts are checked for existence.
#
# Default: 'media'.
whisparr_srv_user: 'media'

# Group that Whisparr will run under.
#
# 'media' will automatically be created using vars/main.yml if required.
# All other accounts are checked for existence.
#
# Default: 'media'.
whisparr_srv_group: 'media'

###############################################################################
# Config
###############################################################################

# Whisparr configuration to set.
#
# Ansible controller path to source configuration files (treated as a jinja2
# templates allowing for ansible variable interpretation). Overwrites existing
# config data in /var/opt/whisparr/*. SQlite databases should be included or use
# restore. Files not sourced will be removed (metadata MediaCover directory
# will not be touched).
#
# A default configuration is autogenerated if not provided.
#
# Use whisparr_flg_backup, whisparr_flg_restore to include MediaCover metadata.
#
# deluge_cfg_dir: 'host_vars/whisparr.example.com/config'
#
# Default: '' (use minimally configured default).
whisparr_cfg_dir: ''

# Role backup location.
#
# All backup operations for a given role execution are stored locally on
# ansible controller with the format 'YYYY-MM-DDTHHMMSS_{FILE}'.
#
# whisparr_cfg_backup_dir: 'data/whisparr'
#
# Default: ''
whisparr_cfg_backup_dir: ''

# Backup metadata (MediaCover) directory?
#
# Multi-GB. Safe to delete. Will cause large backup/restore transfers or
# re-download of metadata when service is restarted. Alternatively externally
# manage /var/opt/whisparr/MediaCover .
#
# Default: True.
whisparr_cfg_backup_metadata: true
